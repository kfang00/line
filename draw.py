from display import *

def draw_line( x0, y0, x1, y1, screen, color ):
    x = x0
    y = y0
    A = 
    B =
    d = 2A + B
    while x <= x1:
        plot (x, y)
        if d > 0:
            y = y + 1
            d = d + 2B
        x = x + 1
        d = d + 2A
    # d = 2A + B
    # while x <= x1:
    #     plot (x, y)
    #     if d > 0:
    #         y = y + 1
    #         d = d + 2B
    #     x = x + 1
    #     d = d + 2A
    # d = 2A + B
    # while x <= x1:
    #     plot (x, y)
    #     if d > 0:
    #         y = y + 1
    #         d = d + 2B
    #     x = x + 1
    #     d = d + 2A
    # d = 2A + B
    # while x <= x1:
    #     plot (x, y)
    #     if d > 0:
    #         y = y + 1
    #         d = d + 2B
    #     x = x + 1
    #     d = d + 2A
